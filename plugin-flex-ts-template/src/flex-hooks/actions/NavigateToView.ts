import * as Flex from '@twilio/flex-ui';

export default (flex: typeof Flex, manager: Flex.Manager) => {
  //beforeNavigateToView(flex, manager);
  //replaceNavigateToView(flex, manager);
  //afterNavigateToView(flex, manager);
}

function beforeNavigateToView(flex: typeof Flex, manager: Flex.Manager) {
}

// Avoid using replce hook if possible
function replaceNavigateToView(flex: typeof Flex, manager: Flex.Manager) {
}

function afterNavigateToView(flex: typeof Flex, manager: Flex.Manager) {
}

