import * as Flex from '@twilio/flex-ui';


export default (flex: typeof Flex, manager: Flex.Manager) => {
  //setDefaultProps(flex, manager);
}

function setDefaultProps(flex: typeof Flex, manager: Flex.Manager) {
}
