import * as Flex from '@twilio/flex-ui';

export default (flex: typeof Flex, manager: Flex.Manager) => {
  //beforeSetActivity(flex, manager);
  //replaceSetActivity(flex, manager);
  //afterSetActivity(flex, manager);
}

function beforeSetActivity(flex: typeof Flex, manager: Flex.Manager) {
}

// Avoid using replace hook if possible
function replaceSetActivity(flex: typeof Flex, manager: Flex.Manager) {
}

function afterSetActivity(flex: typeof Flex, manager: Flex.Manager) {
}
