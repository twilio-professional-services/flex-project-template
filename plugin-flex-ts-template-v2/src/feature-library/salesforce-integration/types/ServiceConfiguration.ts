export default interface SalesforceIntegrationConfig {
  enabled: boolean;
  activity_logging: boolean;
  click_to_dial: boolean;
  copilot_notes: boolean;
  hide_crm_container: boolean;
  prevent_popout_during_call: boolean;
  screen_pop: boolean;
}
